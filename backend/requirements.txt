fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
python-multipart>=0.0.9
reportlab>=4.2.0
spacy>=3.7.0
python-dotenv>=1.0.0
pypdf>=4.3.0

# Advanced document parsing (mode switcher: general documents, tables, images)
pdfplumber>=0.11.0      # PDF table extraction
python-docx>=1.1.0      # Word (.docx) documents
python-pptx>=1.0.0      # PowerPoint (.pptx) slide decks
openpyxl>=3.1.0         # Excel (.xlsx) spreadsheets
Pillow>=10.0.0           # image decoding for OCR
pytesseract>=0.3.10      # OCR (requires the `tesseract-ocr` system package)

# Database (Postgres — required for persistent users + per-user session history)
asyncpg>=0.29.0

# Auth (JWT + password hashing)
pyjwt>=2.9.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.1,<5.0.0
email-validator>=2.2.0

# Rate limiting (optional but recommended)
slowapi>=0.1.9
